#include<stdio.h>
int main() {
    int length=10, breadth=5;
    int area= length*breadth;
    int perimeter= 2*(length+breadth);
    printf("area=%d\n",area);
    printf("perimeter=%d\n",perimeter);
    return 0;
}
